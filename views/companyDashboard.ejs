 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Company Dashboard - Nerdigo</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1e1e1e;
            color: #fff;
            text-align: center;
        }
        .dashboard {
            padding: 50px;
        }
        h1 {
            color: #f1c40f;
        }
        form {
            background: #34495e;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 2px 2px 10px rgba(0,0,0,0.3);
            max-width: 400px;
            margin: auto;
        }
        input, textarea, button {
            width: 100%;
            margin: 10px 0;
            padding: 10px;
            border: none;
            border-radius: 5px;
        }
        button {
            background-color: #f1c40f;
            color: #1e1e1e;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s ease;
        }
        button:hover {
            transform: translateY(-3px);
        }
        .job-listings {
            margin-top: 50px;
        }
    </style>
</head>
<body>
    <div class="dashboard">
        <h1>Company Dashboard</h1>
        <form action="/post-job" method="POST">
            <input type="text" name="company" placeholder="Company" required>
            <input type="text" name="title" placeholder="Job Title" required>
            <textarea name="description" placeholder="Job Description" required></textarea>
            <input type="text" name="location" placeholder="Location" id="">
            <input type="text" name="type" placeholder="Internship/Job" id="">
            <input type="url" name="link" placeholder="Application Link" required>
            <button type="submit">Post Job</button>
        </form>
    </div>

    <div class="job-listings">
        <h2>Posted Jobs & Internships</h2>
        <ul id="job-list">
            <!-- Job listings will be populated here -->
        </ul>
    </div>
</body>
</html>
